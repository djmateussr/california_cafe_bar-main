<!DOCTYPE html>
<html>
  <head>
    <title>California Cafe Bar</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="icon" href="<%= asset_path 'favicon.ico' %>" type="image/x-icon">

    <style>
      body {
        background-color: #380447;
        margin: 0;
        padding: 0;
        color: purple; /* Opcional: melhora contraste com o fundo escuro */
      }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
